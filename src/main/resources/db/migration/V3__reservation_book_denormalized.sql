-- Los libros (libros) viven exclusivamente en MongoDB desde V1.
-- La tabla reservaciones ya fue creada con book_id VARCHAR(36) y todas
-- las columnas desnormalizadas necesarias desde el inicio.
-- Esta migración existe como placeholder de versión.
SELECT 1;
